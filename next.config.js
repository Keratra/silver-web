/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: false,
	// swcMinify: true,
	images: {
		domains: ['ahmetyildiz.info', 'i.picsum.photos'],
	},
};

module.exports = nextConfig;
